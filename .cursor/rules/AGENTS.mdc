---
description: 聊天滚动交互约束，避免自动滚动与手动滚动冲突
alwaysApply: true
---

# 聊天滚动交互规则

处理本项目的聊天滚动、自动滚动、回到底部按钮、移动端滚动恢复时，先遵守以下约束：

1. 自动滚动不能与手动滚动抢控制权。
   - 用户只要正在手动浏览历史消息，自动滚动就必须立即让出控制权。
   - 不要引入任何“猜测用户已经结束交互，所以主动抢回控制权”的逻辑。
   - 如果某个方案可能在用户仍在滑动、惯性滚动尚未稳定、或手动浏览意图仍然成立时恢复自动滚动，就不要这样做。

2. 不要把非确定性的交互结束事件作为核心恢复依据。
   - `touchend`、`touchcancel`、`pointerup`、类似的结束事件，都不能作为恢复自动滚动的主要真相来源。
   - 这些事件最多只能作为辅助信息，不能作为核心判断条件。
   - 如果方案本质上依赖这些事件来恢复自动滚动，视为不符合要求。

3. 优先依据位置事实，不要依据手势猜测。
   - 优先使用稳定事实：当前滚动位置、距底部距离、是否明确点击“回到底部”、是否明确发送消息后需要跟随等。
   - 不要因为“计时器到期了”“像是结束了”“大概率已经停下了”就推断用户交互已经结束。
   - 恢复逻辑应建立在可验证的位置事实和明确意图上，而不是事件时序猜测上。

4. 修改滚动逻辑前，先复述这些约束，再检查方案是否违反它们。
   - 先明确说明：不会和手动滚动抢控制权。
   - 先明确说明：不会依赖 `touchend` / `touchcancel` / `pointerup` 作为核心依据。
   - 先明确说明：恢复依据是什么位置事实或显式意图。

5. 优先做最小改动。
   - 不要在未被要求时重写整套滚动状态机。
   - 保持现有交互模型、现有功能边界、现有按钮语义不变，除非用户明确要求重构。
   - 修复应尽量局部、可解释、可回退。

6. 回答这类问题时，先解释“为什么会这样”，再提方案。
   - 先区分“根因”和“现象”。
   - 如果一个方案仍然存在与手动滚动争抢控制权的风险，要明确指出，而不是直接实施。
